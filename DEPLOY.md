# 🚀 Деплой на Railway

## Быстрый деплой

### Шаг 1: Создай аккаунт на Railway
1. Перейди на https://railway.app/
2. Зарегистрируйся (можно через GitHub)

### Шаг 2: Создай новый проект
1. Нажми "New Project"
2. Выбери "Deploy from GitHub repo"
3. Если репозиторий не на GitHub, выбери "Empty Project"

### Шаг 3: Деплой

#### Вариант А: Через GitHub (рекомендуется)
1. Залей код на GitHub:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/твой-username/твой-репо.git
git push -u origin main
```

2. В Railway:
   - Выбери свой репозиторий
   - Railway автоматически обнаружит проект и начнет деплой
   - Подожди пару минут

3. Получи публичный URL:
   - Открой свой проект
   - Перейди в Settings → Networking
   - Нажми "Generate Domain"
   - Скопируй URL (например: `your-app.up.railway.app`)

#### Вариант Б: Через Railway CLI
1. Установи Railway CLI:
```bash
npm install -g @railway/cli
```

2. Залогинься:
```bash
railway login
```

3. Инициализируй проект:
```bash
railway init
```

4. Задеплой:
```bash
railway up
```

5. Открой проект:
```bash
railway open
```

### Шаг 4: Настрой переменные окружения (если нужно)
В Railway Dashboard → Variables можно добавить:
- `PORT` - Railway автоматически устанавливает порт
- Другие переменные при необходимости

### Шаг 5: Готово! 🎉
Твое приложение доступно по адресу, который Railway предоставил!

## ⚠️ Важно

- **HTTPS**: Railway автоматически предоставляет HTTPS (нужен для камеры/микрофона)
- **WebSocket**: Railway поддерживает WebSocket из коробки
- **Бесплатный план**: Есть лимиты, но для личного использования достаточно
- **Автообновление**: При пуше в GitHub приложение автоматически обновится

## 🔧 Проблемы?

### Приложение не запускается
1. Проверь логи: Railway Dashboard → Deployments → View Logs
2. Убедись, что все зависимости установлены
3. Проверь, что в `package.json` правильный `start` скрипт

### WebSocket не работает
- Railway поддерживает WebSocket, проблем быть не должно
- Проверь, что используешь правильный URL (с https://)

### Камера/микрофон не работает
- Убедись, что используешь HTTPS (Railway дает автоматически)
- Проверь разрешения в браузере

## 💡 Советы

1. **Custom Domain**: Можешь подключить свой домен в Settings → Networking
2. **Мониторинг**: Railway показывает метрики использования
3. **Логи**: Все логи доступны в реальном времени
4. **Переменные**: Можно добавить секреты и конфиги

---

**Удачи с деплоем!** 🚀

